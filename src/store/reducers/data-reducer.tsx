import { ActionType } from "../action";

const initialState = {
  contacts: []
};

const dataReducer = (state = initialState, action) => {
  switch (action.type) {
    // case :
    //   return ;
    default:
      return state;
  }
};

export {dataReducer};